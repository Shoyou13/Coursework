@model IEnumerable<DvdStore.Models.MemberCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <a href="@Url.Action("Create","MemberCategories")" class=" btn btn-success" style="margin-bottom:10px"><i class="fa fa-plus" style="margin-right:8px"></i>Add New Member</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MemberType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalLoan)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MemberType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TotalLoan)
        </td>
        <td>
            <a href="@Url.Action("Edit", new { id=item.MemberCatID })" class="btn btn-default"><i class="fa fa-pencil" style="margin-right:5px"></i>Edit</a>
            <a href="@Url.Action("Details", new { id=item.MemberCatID })" class="btn btn-default" style="margin-left:8px">Details</a>
            <a href="@Url.Action("Delete", new { id=item.MemberCatID })" class="btn btn-danger" style="margin-left:8px"><i class="fa fa-trash" style="margin-right:5px"></i>Delete</a>
        </td>
    </tr>
}

</table>
<link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" />